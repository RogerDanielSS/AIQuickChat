// Tipos globais do Expo (CSS modules, assets, etc.).
// Fica fora do expo-env.d.ts raiz porque o Expo CLI gerencia (e pode remover)
// aquele arquivo em `expo start`/`expo export`.
/// <reference types="expo/types" />
